<input
type="text"
value="{{ $value }}"
name="{{ $name }}"
placeholder="{{ $placeholder }}"
class="border border-gray-300 rounded-lg px-3 py-1 w-full focus:outline-none focus:ring-2 focus:ring-blue-500">
